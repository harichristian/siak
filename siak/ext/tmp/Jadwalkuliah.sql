ALTER TABLE TJADKULDETIL DROP CONSTRAINT TJADKULDETIL_PK;

ALTER TABLE TJADKULDETIL DROP CONSTRAINT TJADKULDETIL_UK;

DROP TABLE TJADKULDETIL;

CREATE TABLE TJADKULDETIL (
		ID INTEGER NOT NULL, 
		HARI_ID INTEGER NOT NULL, 
		SESI_ID INTEGER NOT NULL, 
		RUANG_ID INTEGER NOT NULL, 
		JKM_ID INTEGER NOT NULL, 
		CLINTASPRODI VARCHAR(1), 
		NJMLSESI DECIMAL(5 , 0), 
		NMAKS DECIMAL(5 , 0), 
		NISI DECIMAL(5 , 0), 
		DCREATEDDATE DATE, 
		CCREATEDBY VARCHAR(10), 
		DUPDATEDDATE DATE, 
		CUPDATEDBY VARCHAR(10)
	)
	DATA CAPTURE NONE 
	IN USERSPACE1
	COMPRESS NO;

ALTER TABLE TJADKULDETIL ADD CONSTRAINT TJADKULDETIL_PK PRIMARY KEY
	(ID);

ALTER TABLE TJADKULDETIL ADD CONSTRAINT TJADKULDETIL_UK UNIQUE
	(ID, 
	 JKM_ID);

---------------------------
--<ScriptOptions statementTerminator=";"/>

ALTER TABLE TABSENMHS DROP CONSTRAINT PK_TABSENMHS;

DROP TABLE TABSENMHS;

CREATE TABLE TABSENMHS (
		ID INTEGER NOT NULL, 
		SEKOLAH_ID INTEGER NOT NULL, 
		PRODI_ID INTEGER NOT NULL, 
		CTERM VARCHAR(4) NOT NULL, 
		MATAKULIAH_ID INTEGER NOT NULL, 
		CKELOMPOK VARCHAR(2) NOT NULL, 
		HARI_ID INTEGER NOT NULL, 
		SESI_ID INTEGER NOT NULL, 
		DTGLKUL DATE NOT NULL, 
		MAHASISWA_ID INTEGER NOT NULL, 
		CABSEN VARCHAR(1), 
		CPERTEMUAN VARCHAR(2), 
		CTHAJAR VARCHAR(8), 
		CSMT VARCHAR(1), 
		DCREATEDDATE DATE, 
		CCREATEDBY VARCHAR(10), 
		DUPDATEDDATE DATE, 
		CUPDATEDBY VARCHAR(10)
	)
	DATA CAPTURE NONE 
	IN USERSPACE1
	COMPRESS NO;

ALTER TABLE TABSENMHS ADD CONSTRAINT PK_TABSENMHS PRIMARY KEY
	(ID);

